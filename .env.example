# ──────────────────────────────────────────────────────────────────────────────
# AI Hero Academy — Local development environment template
# Copy this file to .env (gitignored) and fill in your values.
#
# This MVP uses Google Cloud Platform services:
# - Gemini AI for LLM calls
# - Firestore for data persistence
# - Cloud Run for deployment
# ──────────────────────────────────────────────────────────────────────────────

# Google Cloud Platform
GCP_PROJECT_ID=your-gcp-project-id
GOOGLE_API_KEY=your-gemini-api-key

# Firestore database
FIRESTORE_DATABASE=(default)

# Gemini AI models
GEMINI_PRO_MODEL=gemini-3.1-pro-preview
GEMINI_FLASH_MODEL=gemini-3-flash-preview

# Development mode: activates test-user identity
# Change DEV_USER_EMAIL to any address — it becomes the isolated test user in Firestore.
LOCAL_DEV=true
DEV_USER_EMAIL=dev@example.com
