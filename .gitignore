.claude/settings.local.json

.databricks

# Local dev environment variables
.env

# Python
.venv/
__pycache__/
*.pyc

# Playwright browser cache
.playwright-mcp/

# AI call logs (local fallback for Phase A)
logs/

# Debug / test artifacts
_setup_result.json
*.png
!assets/screenshots/*.png
content_test/
console-warnings.log

# Windows shell artifacts
nul

# GCP local credentials — service-account keys must never be committed
.gcp/

# Sensitive local files — never commit
uat_token.txt
proxy-cert.pem

# Claude Code machine-specific settings
.claude/settings.json

# Temp atom generation artifacts
tmp_*.json
